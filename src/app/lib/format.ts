/**
 * TruthBounty Protocol V2 — UI Formatting Utilities
 *
 * Re-exports the canonical formatting engine from `@/lib/format`.
 * Retained for backwards-compatibility across the app directory.
 */

export * from "@/lib/format";
